"""
Below we've defined our first, very simple function! :-)
- The function simply prints the string “hi!”
- NOTE: Functions are written towards the top of your file and every function
requires 2 lines of padding around it unless you wrote a function at the very
top of your file on line 1 however, in this case, you would still need 2 lines
of padding underneath the function
"""


def say_hi():
    print("hi!")


# To run/call/invoke the function, you just need to type the name of the
# function followed by empty parentheses - If you don’t include the
# parentheses, it won't run/call/invoke the function
say_hi()
